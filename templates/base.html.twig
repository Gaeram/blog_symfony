<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    {% block title %}
        <title>Blog de la Piscine</title>
    {% endblock %}
</head>
<body class="js-body">
    <header>
        <form method="get" action="{{ path('admin-articles-search') }}">
            <input type="text" name="search">
            <input type="submit">
        </form>
        <nav class="burger-menu">
            <ul class="flexible">
                <li><a href="{{ path('home') }}">Accueil</a></li>
                <li><a href="{{ path('articles') }}">Articles</a></li>
                <li><a href="{{ path('categories') }}">Catégories</a></li>
            </ul>
        </nav>
        <button type="button" class="nav-toggler">
            <span class="line l1"></span>
            <span class="line l2"></span>
            <span class="line l3"></span>
        </button>
        <button class="js-night-toggle">Dark</button>
    </header>
    <main>
        {% block main %}


        {% endblock %}
    </main>

    <footer>
        {% block footer %}
            <p>Blog de la piscine</p>
        {% endblock %}
    </footer>
<script src="{{ asset('assets/js/main.js') }}"></script>
</body>
</html>
