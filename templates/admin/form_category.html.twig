{% extends 'admin/base.html.twig' %}

{% block main %}
  {#  <form action="{{ path('admin-insert-category') }}" method="get">
        <label for="title">Titre</label>
        <input type="text" name="title">

        <label for="description">Description</label>
        <input type="text" name="description">

        <label for="color">Couleur</label>
        <input type="color" name="color">

        <label for="submit">Envoyer</label>
        <input type="submit">
    </form>#}

   {# {{ form(form) }} #}

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_label(form.title, 'Titre', {'label_attr': {'class': 'form-category-title-label'}}) }}
            {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group">
            {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-category-description-label'}}) }}
            {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group">
            {{ form_label(form.color, 'Publier', {'label_attr': {'class': 'form-category-color-label'}}) }}
            {{ form_widget(form.color, {'attr': {'class': 'form-control'}}) }}
        </div>
         {{ form_widget(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
{% block footer %}
    <p>je suis le footer de la page de création de catégories</p>
{% endblock %}
